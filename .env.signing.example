# Credenziali per firmare l'APK Android di release.
# Copia questo file in `.env.signing` (ignorato da git) e compila i valori.
# Usato da client/scripts/build-apk.sh per firmare con bank-monitor-release.jks.

KEY_ALIAS=bank-monitor
KEYSTORE_PASSWORD=changeme
KEY_PASSWORD=changeme
